// --- IMPORTANT ---
// If you are running the app on a physical device (and not an emulator),
// you must replace 'localhost' with your computer's IP address.
// You can find your IP address by running `ipconfig` (on Windows) or `ifconfig` (on Mac/Linux).
const IP_ADDRESS = 'localhost';
// -----------------

const API_URL = 'http://192.168.1.11:3000';
const SOCKET_URL = 'http://192.168.1.11:3000';

export { API_URL, SOCKET_URL };
